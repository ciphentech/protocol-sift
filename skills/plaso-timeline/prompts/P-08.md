







## P-08 — `SKILL.md` self-correction section + `global/CLAUDE.md` routing

**Goal:** Wire the agent instructions into the Protocol-SIFT routing system.
Update the `plaso-timeline` skill to hand off to NTP enrichment.

**Prompt:**

````
Work inside protocol-sift/. Two existing files need updating — append only.

1. skills/plaso-timeline/SKILL.md
   After "### 4. Filter & Export" and before "### 5. Merging", insert:

   ### 4b. NTP Time Enrichment (agent-driven, post-export)
   After psort.py export, the agent autonomously runs NTP enrichment:
   ```
   # The agent reads the NTP skill and executes its workflow end-to-end
   # @~/.claude/skills/ntp-enrichment/SKILL.md
   ```
   The NTP skill handles source resolution, field computation, manifest
   evaluation, and self-correction — no human input needed between steps
   unless EID logs are absent and the analyst must supply the NTP source.
   Sort all subsequent analysis on `nist_time`, not on `date`/`time`.

2. global/CLAUDE.md — Tool Routing table, add row:
   | NTP time enrichment | `@~/.claude/skills/ntp-enrichment/SKILL.md` |

3. global/settings.json — permissions block, add to "allow":
   "Write(./analysis/*_ntp_manifest*.json)",
   "Write(./analysis/*_ntp_caveats.txt)",
   "Bash(python analysis-scripts/ntp_resolver.py *)",
   "Bash(python analysis-scripts/ntp_enricher.py *)"

   These are the minimal additions needed. Do NOT broaden existing
   permissions or remove any deny entries.

4. Extend the Stop hook command in global/settings.json to capture the NTP
   manifest path in the audit log when present:
     existing_command && \
     (ls ./analysis/*_ntp_manifest*.json 2>/dev/null | \
      xargs -I{} echo "$(date -u): [ntp-enrichment] manifest: {}" \
      >> ./analysis/forensic_audit.log) || true

   Wrap in || true. Never break session shutdown.

Acceptance:
  grep "ntp-enrichment" global/CLAUDE.md
prints the routing row.
  grep "ntp_resolver" global/settings.json
prints the new Bash allow entry.
  grep "ntp_manifest" global/settings.json
prints the new Write allow entry.
  python -c "import json; s=json.load(open('global/settings.json'));
    allow=s['permissions']['allow'];
    assert any('ntp_enricher' in a for a in allow), 'missing enricher allow';
    deny=s['permissions']['deny'];
    assert any('rm -rf' in d for d in deny), 'deny list corrupted';
    print('settings OK')"
prints "settings OK".
````

**Acceptance check:**

````bash
grep "ntp-enrichment" global/CLAUDE.md
# Must show routing row

python -c "
import json
s = json.load(open('global/settings.json'))
allow = s['permissions']['allow']
deny  = s['permissions']['deny']
assert any('ntp_enricher' in a for a in allow), 'missing enricher allow'
assert any('ntp_resolver' in a for a in allow), 'missing resolver allow'
assert any('rm -rf' in d for d in deny),        'deny list corrupted — stop'
print('settings.json OK')
"

# Full unit test regression
pytest tests/ -q
# Must show: 28 passed (24 original + 4 manifest tests)
````

---