## P-10 — `install.sh` patch + final verification

**Goal:** Update installation artifacts and run the complete verification
suite.

**Prompt:**

````
Work inside protocol-sift/. Update installation artifacts and run the
complete verification suite.

1. requirements.txt — append:
   # NTP enrichment
   ntplib>=0.4.0

2. install.sh — after the existing skills copy block, add the ntp-enrichment
   skill + analysis scripts install block (same pattern as existing skills):
   info "Installing ntp-enrichment skill…"
   mkdir -p "$CLAUDE_DIR/skills/ntp-enrichment"
   cp "$REPO_DIR/skills/ntp-enrichment/SKILL.md" \
      "$CLAUDE_DIR/skills/ntp-enrichment/SKILL.md"
   ok "  skills/ntp-enrichment/SKILL.md installed"
   for script in ntp_resolver.py ntp_enricher.py; do
       cp "$REPO_DIR/analysis-scripts/$script" \
          "$CLAUDE_DIR/analysis-scripts/$script"
       ok "  analysis-scripts/$script installed"
   done

3. analysis-scripts/tests/verify_install.sh — 8 assertions:
   (a) skills/ntp-enrichment/SKILL.md exists
   (b) analysis-scripts/ntp_resolver.py exists
   (c) analysis-scripts/ntp_enricher.py exists
   (d) "ntplib" in requirements.txt
   (e) "ntp-enrichment" in install.sh
   (f) "ntp-enrichment" in global/CLAUDE.md (routing entry)
   (g) "ntp_enricher" in global/settings.json (allow entry)
   (h) "rubric_pass" in skills/ntp-enrichment/SKILL.md (agent loop present)
   Each echoes [PASS]/[FAIL]. Exit non-zero on first FAIL.

Acceptance:
  bash analysis-scripts/tests/verify_install.sh
8 PASS lines, exit 0.

Final full suite:
  pytest tests/ -v && \
  bash analysis-scripts/tests/smoke_ntp_agent.sh && \
  bash analysis-scripts/tests/verify_install.sh
All three commands exit 0.
````

**Acceptance check:**

````bash
bash analysis-scripts/tests/verify_install.sh
# Must show: 8 PASS lines, exit 0

pytest tests/ -v && \
bash analysis-scripts/tests/smoke_ntp_agent.sh && \
bash analysis-scripts/tests/verify_install.sh
# All three must exit 0
````
